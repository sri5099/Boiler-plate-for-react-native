import {View, Text} from 'react-native';
import React from 'react';
import {FONTS, ICON, TITLE} from '@/constants';

const INITIALSCREEN = () => {
  return (
    <View>
      <Text>INITIALSCREEN</Text>
    </View>
  );
};

export default INITIALSCREEN;
